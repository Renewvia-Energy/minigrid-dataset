# ── MySQL database ──────────────────────
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_DATABASE=
DB_PORT=

# ── Anonymization ────────────────────────
# 256-bit salt for SHA-256 pseudonymization of customer identifiers.
# Keep this secret and offline — never commit the real value.
ANON_SALT=
